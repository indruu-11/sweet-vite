[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so client-side routes (e.g. /menu, /order, /status, /admin) work on refresh
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
